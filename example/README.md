# example

Example Flutter app for `flutter_adaptive_scaffold`.

The sample shows:

- multiple navigation tabs driven by `AdaptiveScaffold`
- `MaterialBuilders` for compact and expanded body layouts
- a secondary pane that appears on wider breakpoints

Run it with:

```bash
cd example
flutter run
```
